// this Jenkins pipeline is for Eureka deployment

pipeline {
    agent {
        label 'k8s-slave'
    }
    stages {
        stage ('Build') {
            steps {
                echo '***********Testing for jenkins*******'
            }
        }
    }
}